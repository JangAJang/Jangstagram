spring:

  datasource:
    url: jdbc:h2:tcp://localhost/~/jangstagram
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
  data:
    web:
      pageable:
        one-indexed-parameters: true